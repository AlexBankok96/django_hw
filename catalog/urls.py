from django.urls import path
from . import views

urlpatterns = [
    # Здесь будут ваши маршруты, например:
    # path('', views.index, name='index'),
]


# urlpatterns = [
#     path('admin/', admin.site.urls),
# ]